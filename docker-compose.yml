name: drizzle

services:
  app:
    build: .
    container_name: api
    command: node .
    tty: true
    environment:
      PORT: 8080
      LOG_LEVEL: info
      LOG_FORMAT: pretty
    ports:
      - "8080:8080"
    volumes:
      - "./database:/usr/src/app/database"
