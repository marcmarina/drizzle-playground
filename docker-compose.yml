name: drizzle

services:
  app:
    build: .
    container_name: api
    command: node .
    environment:
      PORT: 8080
    ports:
      - "8080:8080"
    volumes:
      - "./database:/usr/src/app/database"
