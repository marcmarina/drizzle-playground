name: drizzle

services:
  app:
    build: .
    command: node .
    tty: true
    stdin_open: true
    env_file: .env
    environment:
      PORT: 8080
      LOG_LEVEL: info
      LOG_FORMAT: pretty
    network_mode: host
    restart: unless-stopped

