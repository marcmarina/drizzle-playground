name: drizzle

services:
  db:
    image: postgres:16.2
    container_name: drizzle_db
    environment:
      POSTGRES_DB: playground
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
